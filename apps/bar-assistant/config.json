{
  "name": "Bar Assistant",
  "id": "bar-assistant",
  "available": true,
  "short_desc": "Self-hosted cocktail recipe and home bar manager.",
  "author": "Karlo Mikus",
  "port": 8585,
  "categories": ["utilities"],
  "description": "Bar Assistant is a self-hosted app for managing your home bar. Track your ingredients, discover which cocktails you can make, and browse recipes through the Salt Rim web client. This app bundles the Bar Assistant API server, the Salt Rim frontend, a Meilisearch search service, and Redis for caching and sessions.",
  "tipi_version": 1,
  "version": "5",
  "source": "https://github.com/bar-assistant/docker",
  "website": "https://barassistant.app",
  "exposable": true,
  "dynamic_config": true,
  "min_tipi_version": "4.0.0",
  "supported_architectures": ["arm64", "amd64"],
  "form_fields": [
    {
      "type": "random",
      "label": "Meilisearch master key",
      "env_variable": "MEILI_MASTER_KEY",
      "encoding": "hex",
      "min": 64,
      "required": true,
      "hint": "Auto-generated secret used to secure the search service. Leave as generated."
    },
    {
      "type": "text",
      "label": "API URL (browser-reachable)",
      "env_variable": "API_URL",
      "placeholder": "http://192.168.1.10:8586",
      "hint": "Full URL your browser uses to reach the Bar Assistant API. Use your Tipi host IP with port 8586.",
      "required": true
    },
    {
      "type": "text",
      "label": "Meilisearch URL (browser-reachable)",
      "env_variable": "MEILISEARCH_URL",
      "placeholder": "http://192.168.1.10:8587",
      "hint": "Full URL your browser uses to reach the search service. Use your Tipi host IP with port 8587.",
      "required": true
    },
    {
      "type": "boolean",
      "label": "Allow new user registration",
      "env_variable": "ALLOW_REGISTRATION",
      "default": true,
      "hint": "Keep on for first launch so you can create your account, then you can turn it off and reinstall/update.",
      "required": false
    }
  ]
}
