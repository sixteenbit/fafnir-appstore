{
  "name": "Opengist",
  "id": "opengist",
  "available": true,
  "short_desc": "Self-hosted pastebin powered by Git — a personal GitHub Gist.",
  "author": "Thomas Miceli",
  "port": 6157,
  "categories": ["development", "utilities"],
  "description": "Opengist is a self-hosted pastebin powered by Git, a lightweight alternative to GitHub Gist. Create, share, and version code snippets; every gist is a Git repository you can clone, pull, and push. Features include syntax highlighting, Markdown and CSV rendering, embeddable gists, OAuth login (GitHub/GitLab/Gitea/OIDC), and full-text search.",
  "tipi_version": 3,
  "version": "1.13.1",
  "source": "https://github.com/thomiceli/opengist",
  "website": "https://opengist.io",
  "exposable": true,
  "dynamic_config": true,
  "supported_architectures": ["arm64", "amd64"],
  "created_at": 1782143885000,
  "updated_at": 1782148337000,
  "form_fields": [
    {
      "type": "random",
      "label": "Session secret key",
      "env_variable": "OG_SECRET_KEY",
      "encoding": "hex",
      "min": 64,
      "required": true,
      "hint": "Auto-generated secret used for the session store and to encrypt MFA data. Leave as generated — pinning it keeps you logged in across restarts/updates."
    },
    {
      "type": "text",
      "label": "Public URL override (proxy/tunnel only)",
      "env_variable": "OG_EXTERNAL_URL",
      "placeholder": "https://gist.example.com",
      "hint": "Leave BLANK — the public URL is derived automatically from your Runtipi domain (LAN host:port, or your exposed domain over HTTPS). Only set this if you front Opengist with an external reverse proxy or tunnel whose hostname differs from the domain Runtipi knows, so OAuth logins, embeds, and clone URLs resolve correctly.",
      "required": false
    }
  ]
}
