{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in */apps/*) bun test;; esac",
            "timeout": 120,
            "statusMessage": "Validating apps with bun test..."
          }
        ]
      }
    ]
  }
}
